# Dockerfile base per tutti i servizi
FROM python:3.11-slim

WORKDIR /app

# Copia requirements e installa dipendenze
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copia il codice
COPY main.py .

# Espone la porta (verrà sovrascritta da ogni servizio)
EXPOSE 8000

# Comando di avvio
CMD ["python", "main.py"]
